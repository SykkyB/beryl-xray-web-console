# Build artifacts
/xray-panel
/dist/
*.exe
*.test
*.out

# Local editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local panel configs that may contain secrets
panel.yaml
panel.local.yaml
disabled.json

# Real sing-box config contains VLESS UUID (auth credential) — keep template only
router/etc/sing-box/config.json
router/etc/sing-box/config.local.json
build/sing-box-build/

# xray-panel-cli build artifacts and local panel config (bcrypt hash)
/dist/
/xray-panel-cli
deploy/panel.yaml
deploy/panel.local.yaml

# Router-state backups (contain VLESS UUIDs and bcrypt hashes)
/backups/

# Reverse-engineering artefacts of GL.iNet's stock SPA — bundles +
# DOM snapshots. Their JS is GL.iNet's, not ours to redistribute.
# RECON.md is OUR analysis notes and stays tracked.
router/recon/spa/
router/recon/dom/
